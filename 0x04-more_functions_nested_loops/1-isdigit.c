#include "main.h"

/**
  * _isdigit - prints the digit
  *@c: the character to print
  *Return: 1 if c is a digit & zero otherwise
  */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
		return (0);

	return (0);
}
