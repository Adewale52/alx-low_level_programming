#include "main.h"

/**
  * mul - multiply two integers
  * @a: the first integer
  * @b: the second integer
  * Return: the result of the multiplication
  */
int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
